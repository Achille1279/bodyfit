name: bodyfit
description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.0

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.10.6
  google_mlkit_pose_detection: ^0.14.0
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/face.png
    - assets/images/profil_droit.png
    - assets/images/profil_gauche.png
    - assets/images/dos.png
